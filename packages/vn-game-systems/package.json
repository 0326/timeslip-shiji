{
  "name": "vn-game-systems",
  "version": "0.1.0",
  "description": "Framework-agnostic game rule systems: save, achievements, death codex, gacha, minigame registry, character codex, stats, settings. Emits domain events via EventBus — UI consumes from outside.",
  "license": "MIT",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": { "test": "vitest run", "test:watch": "vitest" },
  "dependencies": {
    "vn-content-schemas": "workspace:*"
  },
  "devDependencies": { "vitest": "^3.0.0", "typescript": "^5.9.3" }
}
